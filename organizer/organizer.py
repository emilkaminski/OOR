#main organizer file

from oclasses.organize import organizer
from oclasses.items import item, note

steve_org = organizer('Steve','A4',40)

#it = item(1,'Item')

#steve_org.open()

new_note = note(1,'First',"this is a very first note added to organizer.")
#print (new_note)
